package randomAnimator;

import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;

abstract class AbstractLogic {

		abstract void randomize();
		
		abstract String stringer(int e, String...strings);
		
		abstract double matters(int... g);
		
		abstract char overloaded(int x);
		
		abstract char overloaded(double y);
		
		abstract char overloaded(String w);
		
		abstract char overloaded(byte... z);
		
		abstract void overloaded(ArrayList c);
		
		abstract Calendar overloaded(Date date);
		
		abstract char overloaded(double y, int d);
		
		abstract char overloaded(int y, double d);
		
		abstract protected char overloaded(boolean c);
}
